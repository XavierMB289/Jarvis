See Computer it runs on
• Setup the output so it puts things in JSON formated "brackets"
• Use the brackets as a baseline to interact with the PC (windows for now)
• Allow the insertion of data from specified files
• Create a pseudocode for output
• Allow the output of the ai to be inserted back into the input with "changes"
Write its own code
• Finalize seeing the computer
• create a json tag that creates a given python file and runs it after user check off
• allow the restarting of Jarvis on ai command
Updated Baselines
• Finalize all the above
• Have the ai check if each file in it's BASELINE folder is the "best it can be"
• Submit the changes with user check off
Fix STT
• create a keyword that is listened for
• When that keyword is heard, send the textline